/**
 * tracking inclusion
 */

function tracker() {
    'use strict';
    var botnet;

    function logError(sLabel, err) {
        imgProcess.errors[sLabel] = err;
        // console.error(imgProcess);
    }
}
